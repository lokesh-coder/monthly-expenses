enum PaymentType { DEBIT, CREDIT, ALL }

enum InputType { CURRENCY, TEXT, NONE }

enum DisplaySize { XS, SM, BASE, MD, LG, XL }
enum AmountDisplayType { CREDIT, DEBIT, PLACEHOLDER, INPUT, SILENT }

enum OrderBy { ASC, DESC }
